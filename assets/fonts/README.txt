Place a fallback UI font here (optional).

Recommended: Inter or Open Sans (SIL/Open Font License).
Save the Regular TTF as `Inter-Regular.ttf` so ThemeManager can auto-load it.

Path example: res://assets/fonts/Inter-Regular.ttf

If no font is present and no `ui.font` is configured, UI will use the engine default font.